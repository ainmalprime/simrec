<h1>Listing recent_activities</h1>

<table>
  <tr>
    <th>Time recorded</th>
    <th>Description</th>
    <th>Resource</th>
    <th>Resource</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @recent_activities.each do |recent_activity| %>
  <tr>
    <td><%= recent_activity.time_recorded %></td>
    <td><%= recent_activity.description %></td>
    <td><%= recent_activity.resource %></td>
    <td><%= recent_activity.resource_id %></td>
    <td><%= link_to 'Show', recent_activity %></td>
    <td><%= link_to 'Edit', edit_recent_activity_path(recent_activity) %></td>
    <td><%= link_to 'Destroy', recent_activity, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Recent activity', new_recent_activity_path %>
